# Add your own rewiews in files placed in lib/reviews ending in .rake,
# for example lib/rewviews/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_tasks
