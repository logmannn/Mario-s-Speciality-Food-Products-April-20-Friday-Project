<h1>Products</h1>
<div class="container">
  <ul>
    <% @products.each do |product| %>
       <li><%= link_to product.name, product_path(product)%> | <%= product.cost %> | <%= product.country_of_origin %></li>
    <% end %>
  </ul>

  <%= link_to "ADD a Product", new_product_path %>
</div>
